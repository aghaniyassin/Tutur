<nav class="navbar navbar-expand-lg navbar-light text-light bg-dark mb-3">
  <a class="navbar-brand text-light" href="#">Tutur</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse " id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <% if logged_in? %>
          <%= link_to 'Sign out', sessions_path, method: :delete, class: 'nav-link text-light' %>
        <% else %>
          <%= link_to 'Sign up', new_users_path, class: 'nav-link text-light' %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
